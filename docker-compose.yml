---
version: '3.9'
services:
  javabuilder:
    env_file:
      - ./javabuild/.env
    volumes:
      - ./javabuild/maven:/opt/maven
      - ./webapps:/opt/webapps
      - ./javabuild/java:/opt/java
    build:
      context: ./javabuild
    image: debian:maven
